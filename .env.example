# Required
DISCORD_TOKEN=
DISCORD_CLIENT_ID=

# Optional (recommended for development)
# DEV_GUILD_ID=123456789012345678

# Optional
# If omitted, defaults to:
# - guild mode when DEV_GUILD_ID is set
# - global mode otherwise
# COMMANDS_MODE=guild

# Optional
# Set to true for ONE run when switching command modes (global <-> guild) to remove duplicates.
# COMMANDS_CLEANUP=false

# Optional
# Where bot state is stored (defaults to ./data). In production, prefer an absolute path on persistent storage.
# DATA_DIR=data

# Optional
# Used as the default server timezone during /setup init (fallback is UTC).
# DEFAULT_TIMEZONE=UTC

